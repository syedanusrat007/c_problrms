#include <stdio.h>
int main()
{
    int a,b,c,sum;
    scanf("%d%d%d",&a,&b,&c);
    switch (1)
    {
    case 1:
        if(a/b)
        printf("y");
        else
            printf("n");
        break;

    }
}
