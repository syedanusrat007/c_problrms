#include <stdio.h>
#include <string.h>

int main()
{
    struct each {
       int nomp;
    int rpo;
    int tnow;
    int tnobo;

    };

struct team {
    char name[50];
    struct each b[50];
}a[50];

    int i,n;
    scanf("%d",&n);
    for(i=0;i<n;i++){
        gets(a[i].name);
        scanf("%d",&a[i].b[i].nomp);
        scanf("%d",&a[i].b[i].nomp);
        scanf("%d",&a[i].b[i].nomp);
        scanf("%d",&a[i].b[i].nomp);
    }

    return 0;
}
